<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="bg-zinc-900 text-white min-h-screen w-full p-7">
        <div class="text-3xl mb-5">Welcome <span class="text-blue-500 capitalize"><%= user.name%></span></div>
        <div class="text-zinc-400">You can create your new post now!</div>
        <div class="form mt-5">
            <form action="/createpost" method="post">
                <textarea rows="6" class="bg-transparent border-[1px] border-zinc-500 outline-none resize-none rounded-md w-1/3 px-3 py-2 block mb-2 text-sm"  name="post" placeholder="somthing to talk about!"></textarea>
                <input type="submit" value="POST" class="bg-blue-500 px-5 py-2 rounded-lg block">
            </form>
        </div>
        <div class="mt-20">
            <h1 class="text-zinc-600 mb-5">Your All Post</h1>
            <div class="flex flex-wrap gap-4">
                <% user.post.reverse().forEach(function(val){ %>
                    <div class="bg-zinc-800 min-h-[20vh] w-1/3 px-4 py-3 rounded-md mb-2 relative border-[1px] border-zinc-700">
                        <h1 class="mb-1 text-blue-500">@<%= user.userName %></h1>
                        <p class=""><%= val.content %></p>
                        <div class="absolute bottom-5 flex item-center gap-3 text-sm">
                            <p class="text-pink-500">Like</p>
                            <a href="/edit/<%=val._id%>" class="text-zinc-500">Edit</a>
                            <!-- <p><%= user.post.date%></p> -->
                        </div>
                    </div>
                <% })%>
            </div>
        </div>
    </div>
</body>
</html>

