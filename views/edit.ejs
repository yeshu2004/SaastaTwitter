<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="bg-zinc-900 text-white min-h-screen w-full p-7">
        <div class="text-3xl mb-5">Welcome back <span class="text-blue-500 capitalize"><%= post.user.name%>!</span></div>
        <div class="text-zinc-400">You can edit your post now!</div>
        <div class="form mt-5">
            <form action="/update/<%= post._id%>" method="post">
                <textarea rows="6" class="bg-transparent border-[1px] border-zinc-500 outline-none resize-none rounded-md w-1/3 px-3 py-2 block mb-2 text-sm"  name="post" placeholder="somthing to talk about!"><%=post.content%></textarea>
                <input type="submit" value="POST" class="bg-yellow-500 text-black px-5 py-2 rounded-lg block">
            </form>
        </div>
    </div>
</body>
</html>

